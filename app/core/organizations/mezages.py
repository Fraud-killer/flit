from bootkit.mezage import Mezage


mzg_unique_org_name_for_owner = (
    Mezage(
        code="unique_org_name_for_owner",
        text="Has already been used by the owner",
    )
)


mzg_org_ref_exist = (
    Mezage(
        code="org_ref_exist",
        text="Must refer to an existing organization record",
    )
)
